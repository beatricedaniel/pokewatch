apiVersion: v1
kind: Secret
metadata:
  name: ml-secrets
  # namespace is set via kubectl -n flag, not hardcoded here
type: Opaque
stringData:
  # Pokemon Price Tracker API key
  POKEMON_PRICE_API_KEY: "pokeprice_pro_69398e532f024347af785658935434dcbc454d50e60f5189"

  # DagsHub credentials
  DAGSHUB_USER_TOKEN: "487b005332f3fa5a515ecaa48d7b546907407c13"
  DAGSHUB_USER_NAME: "beatricedaniel"

  # MLflow tracking URI (DagsHub MLflow server)
  MLFLOW_TRACKING_URI: "https://dagshub.com/beatricedaniel/pokewatch.mlflow"

  # MLflow tracking credentials (for authentication)
  MLFLOW_TRACKING_USERNAME: "beatricedaniel"
  MLFLOW_TRACKING_PASSWORD: "487b005332f3fa5a515ecaa48d7b546907407c13"

---
# ⚠️  WARNING: This file contains real secrets!
#
# This file should be in .gitignore and NEVER committed to Git!
#
# Current secrets are loaded from .env file:
# - POKEMON_PRICE_API_KEY: Pokemon Price Tracker API key
# - DAGSHUB_USER_TOKEN: DagsHub authentication token
# - DAGSHUB_USER_NAME: beatricedaniel
# - MLFLOW_TRACKING_URI: DagsHub MLflow server URL
# - MLFLOW_TRACKING_USERNAME: beatricedaniel
# - MLFLOW_TRACKING_PASSWORD: Same as DAGSHUB_USER_TOKEN
#
# To apply secrets to Kubernetes:
#   kubectl apply -f k8s/ml-secrets-live.yaml -n airflow
#   kubectl apply -f k8s/ml-secrets-live.yaml -n pokewatch  # If using pokewatch namespace
#
# To verify secrets were created:
#   kubectl get secrets -n airflow | grep ml-secrets
#   kubectl describe secret ml-secrets -n airflow
#
# To view secret values (base64 encoded):
#   kubectl get secret ml-secrets -n airflow -o yaml
#
# To decode a secret value:
#   echo "BASE64_VALUE" | base64 -d
#
# For production, use a secret management tool:
#   - HashiCorp Vault
#   - AWS Secrets Manager
#   - Google Secret Manager
#   - Azure Key Vault
#   - Kubernetes External Secrets Operator
